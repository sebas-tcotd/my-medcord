import { createAction } from '@ngrx/store';

export const viewportMobile = createAction('[Viewport] VIEWPORT_MOBILE');

export const viewportTablet = createAction('[Viewport] VIEWPORT_TABLET');

export const viewportDesktop = createAction('[Vierport] VIEWPORT_DESKTOP');
